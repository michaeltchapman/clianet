from clianet.util import run_ansible

class CumulusDriver:
  def add_vlan(vlan, port):
    print "Adding vlan to port via cumulus driver"
  def remove_vlan(vlan, port):
    print "Removing vlan from port via cumulus driver"
